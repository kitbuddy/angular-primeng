import { GetComponentNameDirective } from './get-component-name.directive';

describe('GetComponentNameDirective', () => {
  it('should create an instance', () => {
    const directive = new GetComponentNameDirective();
    expect(directive).toBeTruthy();
  });
});
