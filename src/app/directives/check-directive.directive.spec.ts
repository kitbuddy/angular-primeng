import { CheckDirectiveDirective } from './check-directive.directive';

describe('CheckDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
