import { PercentagePipePipe } from './percentage-pipe.pipe';

describe('PercentagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
